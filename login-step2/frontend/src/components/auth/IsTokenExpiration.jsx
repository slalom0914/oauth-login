import React from 'react'

const IsTokenExpiration = (token) => {
  console.log(token)
  // 토큰 상태값을 관리하기
  // 토큰 만료 1분전에 사용자가 응답이 없으면 로그인 연장하시겠습니까?
  // 사용자가 연장해주세요? - 리프레쉬 토큰을 이용해서 엑세스 토큰 갱신하기
  return (
    <div>
      여기서 화면의 역할은 현재는 필요없다.
    </div>
  )
}

export default IsTokenExpiration

